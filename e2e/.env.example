# Base URLs for each FieldMark backend (match docs/FieldMark_Project_Level_Playwright_E2E_Harness.md).
# Copy to `.env` in this folder if you want overrides without exporting in the shell.

DOTNET_URL=http://localhost:5000
DJANGO_URL=http://localhost:8000
FIBER_URL=http://localhost:3000
