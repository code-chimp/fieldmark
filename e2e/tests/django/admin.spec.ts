import { test } from '../../fixtures/base';

test.describe
  .skip('Django admin', () => {
    test('deferred until Django admin E2E is required', async () => {
      // Intentionally empty.
    });
  });
