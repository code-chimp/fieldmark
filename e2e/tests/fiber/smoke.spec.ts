import { test } from '../../fixtures/base';

test.describe
  .skip('Fiber-only smoke', () => {
    test('deferred until Fiber-specific flows diverge from shared specs', async () => {
      // Intentionally empty.
    });
  });
