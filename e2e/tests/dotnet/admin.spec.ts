import { test } from '../../fixtures/base';

test.describe.skip('.NET admin / platform', () => {
  test('deferred until ASP.NET Identity / admin surfaces exist', async () => {
    // Intentionally empty — placeholder for backend-specific admin flows.
  });
});
