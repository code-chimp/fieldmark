@*
    FlashRegion — canonical HTMX target #flash-region (UX-DR14, UX-DR32, D11).
    Polite live region for non-blocking system announcements. Empty by default at
    story 1.5; future stories will populate it from TempData["FlashMessages"].
*@
<div id="flash-region" role="status" aria-live="polite" aria-atomic="false"></div>
